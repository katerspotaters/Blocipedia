<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1">
</head>
  <body>
    <h1>Welcome to Blocipedia!</h1>
      <p>Blocipedia is a project that mimics wikipedia for learning purposes.</p>
      <p>Create and share markdown wikis!</p>
    <p>
     <% if current_user %>
       <p>Welcome back!</p>
      <%= link_to "Create Wiki", new_wiki_path, class: 'btn btn-primary' %>
    <% else %>
        <%= link_to "Sign In", new_user_session_path, class: 'btn btn-primary' %> or
        <%= link_to "Sign Up", new_user_registration_path, class: 'btn btn-primary' %> today!
    <% end %>
    </p>

    <div>
    <% if current_user %>
      <% if current_user.has_role? :premium %>
        <h5>Thanks for being a premium user!</p>
        <%= link_to "Downgrade", downgrade_user_path, method: :post, class: "btn btn-danger", data: {confirm: "Are you sure you want to downgrade your account?"} %>
      <% else %>
        <%= link_to "Upgrade to Premium Today!", new_charge_path, class: "btn btn-success" %>
      <% end %>
    <% end %>
    </div>
  </div>

      <div>
        <p> @copyright Katelyn Hertel 2016 </p>
      </div>
  </body>
</html>
